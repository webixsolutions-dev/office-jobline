# Office Jobline frontend — copy to .env and adjust

VITE_API_URL=http://localhost:4000/api/v1
VITE_SITE_SLUG=office-jobs

# Optional: Supabase client (resume upload to storage bucket `resumes`)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
